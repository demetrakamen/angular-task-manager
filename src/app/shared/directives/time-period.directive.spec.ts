import { TimePeriodDirective } from './time-period.directive';

describe('TimePeriodDirective', () => {
  it('should create an instance', () => {
    const directive = new TimePeriodDirective();
    expect(directive).toBeTruthy();
  });
});
